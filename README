**Task**
TO create an interactive navigation menu that changes color or style
when scrolled or when hovering over a menu item. with fixed navigation 
menu on all pages. 
**By using**
**HTML** to structure the menu
**CSS** to style it
**JavaScript** to add interactivity, such as changing the background color 
when a menu item is hovered over. 